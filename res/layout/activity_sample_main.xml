<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SampleMainActivity" >

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textColor="#0000FF"
        android:text="@string/hello_world" />
    
    <LinearLayout
        android:id="@+id/btnLayout"
        android:layout_width="match_parent"
        android:layout_height="200sp"
        android:orientation="horizontal"
        android:layout_below="@+id/tvTitle">
    	<Button
    	    android:id="@+id/btnZPinSDKTest"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:text="@string/zpinsdktest"
	        android:layout_weight="1"/>
    	<Button 
    	    android:id="@+id/btnZCryptoSDKTest"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:text="@string/zcryptosdktest"
	        android:layout_weight="1"/>
    	<Button 
    	    android:id="@+id/btnZKeyServerSDKTest"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:text="@string/zkeyserversdktest"
	        android:layout_weight="1"/>
    </LinearLayout>
    <EditText 
   	    android:id="@+id/etOutput"
        android:layout_width="match_parent"
        android:layout_height="300dip"
        android:layout_below="@+id/btnLayout"
        android:textSize="16sp"
        android:inputType="none"
        android:focusable="false" />

</RelativeLayout>
